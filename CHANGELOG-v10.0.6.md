# Cloud WG Panel v10.0.6

Emergency stability hotfix:

- Prevents the dashboard from importing peers and rebuilding many iptables rules on every page load.
- Restores Python 3.6 compatibility by replacing `Path.unlink(missing_ok=True)`.
- Makes the desktop sidebar vertically scrollable so Maintenance, Updates, Languages, and Logout remain reachable.
- Includes all v10.0.5 WinBox/BYPASS changes.
